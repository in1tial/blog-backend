<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleMapper">
    <sql id="articleColumn">
        article_id, article_title, article_content, article_subject, article_category, article_tags, create_date , update_date
    </sql>
    <resultMap id="articleMap" type="com.blog.pojo.Article">
        <result property="articleId" column="article_id"/>
        <result property="articleTitle" column="article_title"/>
        <result property="articleSubject" column="article_subject"/>
        <result property="articleContent" column="article_content"/>
        <association property="meta" javaType="com.blog.pojo.Meta">
            <result property="articleCategory" column="article_category"/>
            <result property="articleTags" column="article_tags"/>
            <result property="createDate" column="create_date"/>
            <result property="updateDate" column="update_date"/>
        </association>
    </resultMap>

    <select id="findArticleList" resultMap="articleMap">
        SELECT 
            <include refid="articleColumn" />
        FROM 
            t_article
    </select>

    <select id="findArticleById" resultMap="articleMap">
        SELECT
            <include refid="articleColumn" />
        FROM
            t_article
        WHERE 
            ARTICLE_ID=#{id}
    </select>

</mapper>